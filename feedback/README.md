# feedback/ — Metrics only, no auto-tuning

Collects: clarifications requested, silent resolutions, human overrides, meta-uncertainty events, domain-uncertainty events, post-hoc corrections, outcomes.

Explicitly NOT built here: automatic threshold/policy self-modification. Review loop is manual: observe → record → analyze → manually change → retest.
